language: python
python:
  - "2.7"
  - "3.4"
  - "3.5"
  - "3.6"
cache: pip
install:
  - pip install -r requirements.txt
script:
  - coverage run --source=password_hashing -m unittest discover tests
after_script:
  - coveralls
